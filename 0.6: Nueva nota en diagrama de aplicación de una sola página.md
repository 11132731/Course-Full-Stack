```mermaid
sequenceDiagram
    participant Form
    participant server

    Form->>server: Write and submit note to server using sendToServer
    activate server
    server->>Form: If the insertion was successful, it responds with code 200 and the note is added to the list using the redrawNotes function.
    deactivate server

    NOTE: Happens asynchronously without needing to reload the HTML, CSS, and JS.
